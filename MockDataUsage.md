Searched for "mock"

Mock APIs/data used here:

1. **Weather**: `weather_service.py` -> `_mock_forecast()`
2. **Satellite (NDVI)**: `services/core.py` -> `mock_ndvi()`
3. **ZK Proofs**: `services/core.py` -> `USE_MOCK_ZKP`
4. **Blockchain**: `services/core.py` -> `USE_MOCK_CHAIN`
5. **SMS (Twilio)**: `sms_service.py` -> `USE_MOCK_SMS` / `_mock_send_sms()`
6. **IVR (Voice)**: `ivr_service.py` -> `USE_MOCK_IVR`
7. **AI Disease Diagnosis**: `diagnosis_service.py` -> `USE_MOCK_CLASSIFIER` / `_mock_classify()`